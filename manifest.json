{
	"manifest_version": 2,

	"name": "Break Time",
	"description": "This extension checks if the user has been actively using the browser for more than 1 hour. If so, it notifies to take a break.",
	"version": "1.0",

	"browser_action": {
		"default_icon": "icon.png"
	},

	"background": {
		"scripts": ["notify.js"],
		"persistent": true
	},

	"permissions": [
		"alarms",
		"idle",
		"notifications"
	]
}